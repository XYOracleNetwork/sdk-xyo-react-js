{
  //"entryPointStrategy": "packages",
  "entryPoints": ["src/index.ts"],
  "includeVersion": true,
  "name": "XYO Platform React SDK",
  "tsconfig": "./tsconfig.typedoc.json"

  // Potentially useful for debugging
  // "logLevel": "Verbose"
}